# ViolaHub analysis worker — REAL model + DSP stack.
# basic-pitch ships the ICASSP-2022 ONNX model; with onnxruntime it runs without
# TensorFlow (leaner CPU image). Swap onnxruntime -> onnxruntime-gpu in the AKS
# GPU image for T4 acceleration; the code is identical.
basic-pitch==0.4.0
onnxruntime==1.19.2
librosa==0.10.2.post1
music21==9.3.0
pretty_midi==0.2.10
soundfile==0.12.1
numpy>=1.24,<2.0
scipy>=1.11
defusedxml==0.7.1

# OMR (PDF/image → MusicXML) — oemer is MIT (heavy pool). pdf2image needs poppler.
oemer==0.1.7
pdf2image==1.17.0

# infra clients
azure-servicebus==7.12.3
azure-storage-blob==12.23.1
azure-identity==1.19.0
azure-messaging-webpubsubservice==1.2.1
psycopg[binary]==3.2.3

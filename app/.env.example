# Copy to .env.local to point the app at a real backend.
# Leave unset for offline mode (the AI record/feedback flow is simulated).
# VITE_API_BASE=http://localhost:8000
# VITE_API_TOKEN=<dev HS256 JWT with sub=<user-uuid>, aud=violahub>
